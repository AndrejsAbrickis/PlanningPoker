version: 2
jobs:
  build:
    docker:
      - image: circleci/node:9.9.0-stretch
    steps:
      - checkout
      - run:
          name: Install
          command: yarn install
      - run:
          name: Test
          command: yarn test